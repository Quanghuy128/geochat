"use client";

/**
 * @deprecated Đã thay bằng HeaderAuth + AuthModal (username + password auth).
 * File giữ lại để tránh lỗi import nếu có code nào chưa cập nhật.
 * Xem: src/components/header-auth.tsx, src/components/auth-modal.tsx
 */
export function AuthPanel() {
  return null;
}
