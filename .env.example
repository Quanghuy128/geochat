# Copy sang .env.local rồi điền giá trị thật. KHÔNG commit .env.local.

# Supabase — lấy từ Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Map: dùng MapLibre GL + tiles demotiles MapLibre (free, KHÔNG cần API key).
# Không còn biến NEXT_PUBLIC_GOOGLE_MAPS_API_KEY.
